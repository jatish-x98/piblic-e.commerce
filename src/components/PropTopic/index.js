const PropTopic = (props) =>{
    return(
        <div>
            <h1>prop elements</h1>
            <h1>{props.name}</h1>
            <p>{props.location}</p>
            <p>{props.designation}</p>
        </div>

    )


}

export default PropTopic